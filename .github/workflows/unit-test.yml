name: Lint and Unit Test

on:
  pull_request:
    paths:
      - '.github/**'
      - 'src/**'
      - 'test/**'
      - .eslintrc.js
      - .node-version
      - package-lock.json
      - package.json
      - tsconfig.json

jobs:
  unit-test-and-lint:
    runs-on: ubuntu-18.04
    steps:
      - uses: actions/checkout@v1
      - uses: actions/setup-node@v1
        with:
          node-version: '12.x'
      - run: npm ci
      - run: npm run lint
      - run: npm test
